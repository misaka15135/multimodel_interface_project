# 三模融合浏览器扩展 — 使用说明

> **适用版本**：`fusion_v1.0` | **演示平台**：B站 (bilibili.com) | **交互范式**：Point + Speak

---

## 1. 安装

### 1.1 加载扩展

1. 打开 Chrome/Edge，地址栏输入 `chrome://extensions/`，回车
2. 右上角打开「**开发者模式**」开关
3. 点击左上角「**加载已解压的扩展程序**」
4. 选择 `fusion_v1.0/` 文件夹，点确定
5. 扩展列表中出现「**多模态交互浏览器助手**」即成功

### 1.2 首次配置

加载后必须做两件事：

| 配置项 | 操作 | 必要性 |
|--------|------|--------|
| DeepSeek API Key | 点击扩展图标 → 语音控制 → API Key 输入框 → 填入 `sk-...` → 保存 | **必须**（否则语音 LLM 兜底不可用，本地命令仍正常） |
| 摄像头权限 | 启动眼动/手势时 Chrome 会弹权限提示 → 点「允许」 | **必须**（否则摄像头模态不可用） |

> DeepSeek API Key 在 [platform.deepseek.com](https://platform.deepseek.com) 免费注册获取。语音的本地命中命令（滚动/刷新/缩放等）不调 API，断网也能用；API Key 仅用于 LLM 兜底复杂命令。

---

## 2. 三个模态快速上手

### 2.1 👁️ 眼动 — 用眼睛控制

**启动步骤：**

1. 点击扩展图标 → 眼动控制区域 → 点「**打开控制窗口**」
2. 控制窗口弹出后 → 点「**启动**」按钮
3. 摄像头开启 → 按屏幕指示标定（看中心→左→右→上→下，各停留 1 秒）
4. 保持控制窗口打开（可最小化，但不要关闭）→ 切回 B站页面

**你在页面上会看到：**
- 🔵 青色十字准星跟随你的视线移动
- 准星移动到按钮/链接上时变绿，并显示元素标签
- 盯着屏幕边缘 → 页面自动向该方向滚动
- 眨眼 → 点击当前注视的元素

**可用控制：**

| 控制项 | 说明 |
|--------|------|
| 指示点开关 | 显示/隐藏屏幕上的十字准星 |
| 眨眼点击 | 长眨眼或双眨眼触发点击 |
| 驻留点击 | 注视同一位置超过设定时间自动点击 |
| 方向滑动 | 注视屏幕边缘自动滚动 |
| 头部灵敏度 | 头部移动映射到光标的速度 |
| 标定 | 重新校准注视点映射 |

**关闭方式：** 关闭控制窗口，或在控制面板中取消「启用网页控制」

---

### 2.2 🖐️ 手势 — 用手势控制

**启动步骤：**

1. 打开任意网页（如 B站）
2. 页面左下角会出现摄像头预览窗口
3. 点击预览下方的「**开启增强手势**」按钮
4. 允许摄像头权限

**手势速查表：**

| 手势 | 手指 | 触发 | 功能 |
|------|------|------|------|
| 🖐️ 五指全伸 + 手向上翻 | 全部伸展 | 持续 0.9s | 页面上滚 |
| 🖐️ 五指全伸 + 手向下翻 | 全部伸展 | 持续 0.9s | 页面下滚 |
| ✌️ V 字 | 食指 + 中指 | 持续 0.9s | 刷新页面 |
| 🖕 中指独伸 | 中指 | 即时 | 播放/暂停视频 |
| 💍 无名指向上 | 无名指 | 即时（2s冷却） | 点赞 |
| 👎 拇指向下 | 拇指下 | 即时（1.5s冷却） | 下一个视频 |
| 👈 食指左指 | 食指左 | 即时（0.4s冷却） | 快退 5 秒 |
| 👉 食指右指 | 食指右 | 即时（0.4s冷却） | 快进 5 秒 |
| 🤘 摇滚手势 | 食指 + 小指 | 即时（1.5s冷却） | 倍速切换 |
| 🖱️ 中指 + 无名指 | 中指无名指 | 持续 0.9s | **自由交互模式** |
| 🤙 小指独伸 | 小指 | 持续 0.9s | 停止识别 |

**页面 UI：**
- 左侧：摄像头预览 + 开关按钮
- 右侧：手指状态条（5 根手指各自伸展程度）+ 动作塔（11 个动作状态指示灯）
- 自由交互模式：🔴 红色光标跟随食指尖，悬停 1.2s 自动点击，点后变青色

**关闭方式：** 伸出小指持续 0.9s，或点击左下角「关闭增强手势」

---

### 2.3 🎤 语音 — 用说话控制

**启动步骤：**

1. 页面右下角找到 🎤 浮动按钮
2. **唤醒词模式**（推荐）：说「**小助手**」→ 听到叮咚声 → 指示灯变绿 → 说指令
3. **直接模式**（关闭唤醒词后）：点麦克风按钮 → 直接说指令

**指示灯含义：**

| 颜色 | 状态 | 说明 |
|------|------|------|
| ⚪ 灰 | 关闭 | 完全不监听 |
| 🔵 蓝 | 待机 (PASSIVE) | 只检测唤醒词"小助手" |
| 🟢 绿 | 激活 (ACTIVE) | 接收所有语音指令，10s 无命令自动回到待机 |

**常用命令速查：**

| 类别 | 说法示例 | 效果 | 反馈标记 |
|------|---------|------|---------|
| 📜 滚动 | "往下滚动" "向下滑" "回到顶部" | 页面滚动 | ⚡本地 |
| 🔍 缩放 | "放大" "字太小了" "恢复大小" | 页面缩放 | ⚡本地 |
| 🔄 导航 | "刷新页面" "后退" "前进" | 浏览器导航 | ⚡本地 |
| 👍 社交 | "点赞" "评论太棒了" | B站互动 | ⚡本地/☁AI |
| 🔍 查找 | "查找人工智能" "搜索联系方式" | 页面内搜索高亮 | ⚡本地 |
| 📖 朗读 | "朗读页面" "别读了" | TTS 朗读/停止 | ⚡本地 |
| 🔗 新建 | "新建标签页" | 打开新标签 | ⚡本地 |
| 🎯 指代 | "点这个" "打开它" | 点击手势/眼动指向的元素 | ⚡本地 |
| ↻ 追问 | "继续" "重复" "撤销" | 上下文记忆 | ⚡本地 |
| ⏹ 停止 | "停止监听" "退下" "休息吧" | 关闭语音 | ⚡本地 |

> ⚡本地 = 纯本地匹配，不调 API，断网也能用；☁AI = DeepSeek LLM 兜底解析

**设置入口：** 点击麦克风旁的 ⚙ 齿轮 → 可配置 API Key、TTS 语音反馈开关、唤醒词模式开关

---

## 3. 融合交互场景（B站演示剧本）

### 场景 0：启动所有模态

```
1. 打开 bilibili.com 任意视频页面
2. 点击扩展图标 → 控制中心
3. 眼动：打开控制窗口 → 启动 → 标定
4. 手势：点击左下角「开启增强手势」
5. 语音：说"小助手"唤醒，或点击麦克风
6. 三个模态状态指示灯全部变绿 🟢
```

### 场景 1 👁️+🎤：眼看 + 嘴说

> 展示「眼动提供目标，语音发出命令」

| 步骤 | 你做什么 | 系统反应 |
|------|---------|---------|
| 1 | 盯着 B站视频下方的 👍 点赞按钮 | 十字准星停在点赞按钮上，准星变绿，标签显示"赞" |
| 2 | 说「**点这个**」 | 点赞按钮被点击，Toast 提示"已点击"，点赞数 +1 |

**融合原理**：Eye 持续写 `gazeTarget`，Voice 的 `click_target` 回退读取 `gazeTarget`，通过 `elementFromPoint` 找到按钮并点击。

### 场景 2 🖐️+🎤：手指 + 嘴说

> 展示「手势提供目标，语音发出命令」；手势优先级 > 眼动

| 步骤 | 你做什么 | 系统反应 |
|------|---------|---------|
| 1 | 做中指 + 无名指手势 → 进入自由交互模式 | 出现 🔴 红色光标跟随食指尖 |
| 2 | 移动食指，将光标移到"下一P"或相关视频卡片上 | 光标悬停在目标上 |
| 3 | 说「**打开它**」 | 目标被点击，跳转到对应视频 |

**融合原理**：Gesture 持续写 `pointerTarget`，Voice 优先取 `pointerTarget`（覆盖 `gazeTarget`），点击手势指向的目标。

### 场景 3 🖐️+🎤：去重演示

> 展示「手势和语音重复命令自动去重」

| 步骤 | 你做什么 | 系统反应 |
|------|---------|---------|
| 1 | 做五指上翻手势(PALM_UP)，触发页面上滚 | 页面向上滚动 |
| 2 | **立即**（500ms 内）说「**向上滚**」 | 没有反应（仲裁跳过） |

**融合原理**：Gesture 先发布 `scroll_up` → MMFusion `_recent` 记录 → Voice 的 `arbitrate` 返回 `{execute: false, reason: 'dedup'}` → 不重复执行。

### 场景 4 🎤 独奏：纯语音

> 展示「语音完全独立工作」

| 步骤 | 你做什么 | 系统反应 |
|------|---------|---------|
| 1 | 说「**往下滚动**」 | 页面下滚，Toast 显示"向下滚动 ⚡本地" |
| 2 | 说「**朗读页面**」 | TTS 开始朗读页面内容 |
| 3 | 说「**别读了**」 | 朗读停止 |
| 4 | 说「**查找弹幕**」 | 页面搜索"弹幕"，匹配高亮 |

### 场景 5 👁️ 独奏：纯眼动

> 展示「眼动完全独立工作」

| 步骤 | 你做什么 | 系统反应 |
|------|---------|---------|
| 1 | 注视屏幕左边缘 | 页面自动向右滚动（边缘触发） |
| 2 | 注视屏幕下边缘 | 页面自动向下滚动 |
| 3 | 注视某个视频卡片 → 眨眼 | 点击目标卡片 |

### 场景 6 🖐️ 独奏：纯手势

> 展示「手势完全独立工作」

| 步骤 | 你做什么 | 系统反应 |
|------|---------|---------|
| 1 | 拇指下翻 | 跳转下一个视频 |
| 2 | 无名指向上 | 点赞 |
| 3 | V 字手势持续 0.9s | 刷新页面 |

### 场景 7 👁️+🖐️+🎤：三模同时在线

> 展示「三模全部激活，互不干扰，协同增强」

| 步骤 | 你做什么 | 系统反应 |
|------|---------|---------|
| 1 | 眼动盯着视频区、手势自由光标指向评论区、说「**往下翻**」 | 页面下滚 |
| 2 | 眼看点赞、手指链接、说「**点这个**」 | 点击链接（手势优先） |
| 3 | 关掉手势（小指退出）、眼看点赞、说「**点这个**」 | 点击点赞（眼动回退） |

---

## 4. 控制中心使用

### 4.1 打开控制中心

点击 Chrome 工具栏的「多模态助手」扩展图标，弹出控制面板。

### 4.2 控制面板功能

| 区域 | 功能 |
|------|------|
| **三模态状态条** | 一眼看清眼动/手势/语音的当前状态（🟢运行/🔵待机/⚪关闭） |
| **眼动控制** | 启动/停止追踪、标定、开关指示点/眨眼/驻留/滚动、调节灵敏度 |
| **手势控制** | 摄像头状态、当前手势名、自由交互模式开关 |
| **语音控制** | 麦克风开关、唤醒词开关、TTS 开关、API Key 配置 |
| **融合状态** | 实时显示 `pointerTarget`（手势）、`gazeTarget`（眼动）、最后命令 |

### 4.3 页面浮动指示器

网页右下角（麦克风按钮上方）有三模状态指示灯：

```
👁️🟢 🖐️🟢 🎤🔵
```

- 鼠标悬停 → 显示详细信息
- 点击 → 展开简易控制面板（快速启停任一模态）

---

## 5. 设置与配置

### 5.1 DeepSeek API Key

```
控制中心 → 语音控制 → API Key 输入框 → 填入 sk-xxx → 保存
```

- 获取地址：https://platform.deepseek.com → 注册 → API Keys
- 存储位置：`chrome.storage.local`（键名 `voice_ext_apikey`）
- **不填也能用**：本地命中的 15+ 常用命令（滚动/刷新/缩放/点赞/朗读等）不调 API

### 5.2 唤醒词模式

```
控制中心 → 语音控制 → ☑ 唤醒词 → 保存
```

- 开启后：说「小助手」激活 → 叮咚提示音 → 绿指示灯 → 开始听指令
- 10 秒无命令自动退回待机（蓝指示灯）
- 关闭后：点击麦克风按钮，直接说指令

### 5.3 TTS 语音反馈

```
控制中心 → 语音控制 → ☑ TTS反馈 → 保存
```

- 默认关闭（安静浏览）
- 开启后：执行操作后语音播报确认（如"已向下滚动"）

### 5.4 眼动灵敏度

在控制窗口中调节：
- **头部灵敏度**（0.35~1.25）：越大头部微动对应光标移动越远
- **平滑**（0.03~0.18）：越大光标越跟手但越抖
- **驻留点击时间**（800~2400ms）：注视多久触发点击

---

## 6. 常见问题

### Q：眼动追踪不准？
- 重新标定（控制窗口 → «标定» 按钮 → 看 5 个方向各 1 秒）
- 确保面部光线充足、摄像头正对脸部
- 调节灵敏度和平滑参数

### Q：手势识别不灵敏？
- 手距摄像头 30-60cm
- 光线充足、手部完整可见
- 手势保持 0.5s 以上

### Q：语音识别不准？
- 嘈杂环境会影响 Web Speech 识别
- 需网络畅通（Web Speech 服务需联网）
- 试说"小助手"唤醒，确认唤醒词识别正常

### Q：「点这个」说"没有可操作的目标"？
- 确认手势自由模式已开启（有红色光标）或眼动已启动（有十字准星）
- 确认光标/准星停在可点击元素上（按钮、链接、输入框等）
- 控制中心的融合状态区可查看 `pointerTarget` / `gazeTarget` 是否有值

### Q：三个模态同时开，电脑卡？
- 手势摄像头 320×240，眼动摄像头也是低分辨率，CPU 占用可控
- 必要时关掉不需要的模态（控制中心一键开关）

### Q：手势和语音的滚动同时触发了？
- 正常情况下 500ms 去重窗口会阻止重复
- 如果间隔超过 500ms 仍发生了，这是预期行为（窗口已过，当作两次独立操作）

---

## 7. 演示检查清单

上课演示前，确认以下各项：

- [ ] 扩展已加载，控制中心能正常打开
- [ ] DeepSeek API Key 已配置
- [ ] 摄像头权限已授权（Chrome 设置 → 隐私与安全 → 网站设置 → 摄像头）
- [ ] 麦克风权限已授权
- [ ] 打开 B站视频页面 (bilibili.com/video/...)
- [ ] 眼动控制窗口已打开并启动、已标定
- [ ] 手势摄像头已开启
- [ ] 语音已唤醒（说"小助手"）
- [ ] 三个模态状态指示灯全绿

---

## 8. 快速参考卡片

```
┌─────────────────────────────────────────────────────┐
│              三模融合 快速参考                         │
├─────────────────────────────────────────────────────┤
│                                                     │
│  启动：Chrome 加载 fusion_v1.0/ → B站 → 控制中心     │
│                                                     │
│  👁️ 眼动 = 注视目标    控制窗口 → 启动 → 标定        │
│  🖐️ 手势 = 选择目标    左下角「开启增强手势」         │
│  🎤 语音 = 命令动作    说"小助手" → 说指令            │
│                                                     │
│  ★ "点这个" = 语音从手势/眼动取目标 → 点击           │
│  ★ 手势目标 > 眼动目标（优先级）                      │
│  ★ 同动作 500ms 内去重（防双触发）                    │
│                                                     │
│  关闭：眼动=关窗口  手势=小指0.9s  语音=说"退下"     │
│                                                     │
└─────────────────────────────────────────────────────┘
```
